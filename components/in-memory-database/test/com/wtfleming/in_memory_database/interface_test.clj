(ns com.wtfleming.in-memory-database.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.wtfleming.in-memory-database.interface :as in-memory-database]))

(deftest dummy-test
  (is (= 1 1)))
